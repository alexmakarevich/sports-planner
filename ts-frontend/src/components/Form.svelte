<script>
  // import { createUser, listUsers } from "../api/users";

  // let name = $state("");
  // let password = $state("");
</script>

<h1>USERS - TEMPLATE</h1>

<!-- <input type="text" bind:value={name} />
<input type="password" bind:value={password} />

<button onclick={() => createUser({ name, password })}>SEND IT</button>

{#await listUsers()}
  <p>loading users...</p>
{:then users}
  <ul>
    {#each users as user}
      <li>{user.name}</li>
    {/each}
  </ul>
{:catch err}
  <p>could not load users. :(</p>
{/await}
<ul></ul> -->
